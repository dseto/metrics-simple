# Docker/Container Environment Variables
# Copy this file to .env and adjust values as needed

# SQLite Database Path (optional - defaults to ./config/config.db)
# Used by API and Runner
# METRICS_SQLITE_PATH=./config/config.db

# OpenRouter API Key (required if AI is enabled)
# Can use either METRICS_OPENROUTER_API_KEY or OPENROUTER_API_KEY
# METRICS_OPENROUTER_API_KEY=sk-or-v1-xxxxxxxxxxxx
# OPENROUTER_API_KEY=sk-or-v1-xxxxxxxxxxxx

# ASP.NET Core Environment (optional - defaults to Production in container)
# ASPNETCORE_ENVIRONMENT=Production

# .NET Environment
# DOTNET_ENVIRONMENT=Production
