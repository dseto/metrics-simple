# SourceRequest
A chamada externa é definida por `ProcessVersion.sourceRequest`, combinada com `Connector.baseUrl`.

Regras:
- path é relativo ao baseUrl
- headers/queryParams do sourceRequest complementam os do connector
